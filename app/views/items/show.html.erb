<h1><%= @item.title %></h1>
<p><%= @item.description %></p>
<p>
    <%= link_to items_path do %>
    <i class="fa fa-chevron-circle-left" aria-hidden="true"></i>
    <% end %>
    
    <%= link_to edit_item_path do %> 
    <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
    <% end %>

    <%= link_to item_path(@item),:data => {:confirm => "Sicher?"} ,:method => :delete do %>
    <i class="fa fa-trash" aria-hidden="true"></i>
    <% end %>
  <!-- link_to "Löschen2", item_path(@item), data: {confirm: "Bist du sicher?"}, method: :delete  -->
</p>

<!-- button_to "Zurück", root_path, method: :get, class: "btn btn-default" -->